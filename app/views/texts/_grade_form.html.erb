<%= simple_form_for [@text.theme, @text] do |f| %>
  <%= f.error_notification %>
  <div class="row mx-1">
  <div class="form-inputs my-2 h6">
    <%= f.input :grade, min: 0, label: "Inserir nota" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "✚", class: "btn-texts btn btn-primary"%>
  </div>
  

  </div>
<% end %>